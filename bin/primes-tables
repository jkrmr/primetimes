#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift \
  File.join File.expand_path("..", File.dirname(__FILE__)), "lib"

require "primetimes"
include Primetimes

table = PrimesTable.new(n: ARGV.first.to_i || 3).table
PrimesTablePrinter.new.print(table)
